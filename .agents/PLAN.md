# Goal

Completed: create a generic skill reference repository at ../skills using the newer local workflow and reusable routing/testing/UI practices. Migrate this consumer to a shared reference without copying app behavior into the shared library. Maintain workflow directly.

# Implementation

- [x] Build compact shared executor/maintenance skills and demand-loaded language-neutral testing, optional UI/architecture references. Keep explicit safety, authorization, delta supervision and DoD contracts.
- [x] Generalize runner: separate shared installation from consuming Git project, model/effort + exact local instruction files from .agents/executor.json or CLI. Stable contract/references before dynamic plan; preserve report recovery and failure containment.
- [x] Migrate consumer to relative shared skill symlink + local profile/AGENTS only. Preserve all seven original rule intents, app boundaries and existing checks. Snapshot replaced files in ignored artifacts.
- [x] Verify isolated CLI fake scenarios for linked skills, arbitrary working directories/spaces, multiple language/UI profiles, routing, secrets/path protections, missing config, report outcomes and recovery. Validate skills, links, no app leakage and context sizes.
- [x] Run consumer consumer-required gates and scoped review; document setup and limitations. No live model A/B costs, commit/push or changes to source applications.

# Progress

Completed: shared fake-CLI suite, two skill validators, link/context checks, independent focused review and consumer-required gates passed. No real model calls, commit or push.
